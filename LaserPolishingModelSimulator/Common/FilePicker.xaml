<UserControl x:Class="LaserPolishingModelSimulator.Common.FilePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LaserPolishingModelSimulator.Common"
             mc:Ignorable="d" x:Name="picker"
             d:DesignHeight="50" d:DesignWidth="300">
    <Grid x:Name="root">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Column="0" IsReadOnly="True" Text="{Binding Path=SelectedFile, ElementName=picker}"
                 IsEnabled="{Binding Path=Enabled, ElementName=picker}" HorizontalContentAlignment="Center"
                 VerticalContentAlignment="Center" HorizontalScrollBarVisibility="Visible"/>
        
        <Button Grid.Column="1" Content="{DynamicResource Browse}" Click="Browse_Click"/>
    </Grid>
</UserControl>
