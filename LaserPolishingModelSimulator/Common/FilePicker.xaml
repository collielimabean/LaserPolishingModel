<UserControl x:Class="LaserPolishingModelSimulator.Common.FilePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LaserPolishingModelSimulator.Common"
             mc:Ignorable="d" x:Name="picker"
             d:DesignHeight="50" d:DesignWidth="300">
    <Grid x:Name="root">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="1" Text="{Binding Path=FilePickerLabel, ElementName=picker}" HorizontalAlignment="Center" VerticalAlignment="Center" Background="White"/>
        
        <TextBox Grid.Column="3" IsReadOnly="True" Text="{Binding Path=SelectedFile, ElementName=picker}"
                 IsEnabled="{Binding Path=Enabled, ElementName=picker}" HorizontalContentAlignment="Center"
                 VerticalContentAlignment="Center" HorizontalScrollBarVisibility="Auto"/>
        
        <Button Grid.Column="4" Content="{DynamicResource Browse}" Click="Browse_Click"/>
    </Grid>
</UserControl>
